# Yew Web App

一个使用 Yew 框架构建的 Rust WebAssembly 应用，展示现代化的 Web 开发实践。

## ✨ 功能特性

- ✅ 路由管理（/home 和 /about 页面）
- ✅ 响应式导航栏
- ✅ 交互式计数器演示
- ✅ 现代化 UI 设计
- ✅ Sass 模块化样式系统
- ✅ 完全使用 Rust 编写
- ✅ 响应式设计，适配移动端
- ✅ 渐变色和动画效果

## 🚀 快速开始

### 前置要求

- Rust 1.92+
- Trunk (Yew 应用构建工具)
- WebAssembly 目标

### 安装 Trunk

```bash
cargo install trunk
```

### 添加 WebAssembly 目标

```bash
rustup target add wasm32-unknown-unknown
```

### 安装 Sass（可选，Trunk 会自动处理）

Trunk 内置了 Sass 编译支持，无需额外安装。

## 🎯 运行应用

### 开发模式（带热重载）

```bash
cd web-app
trunk serve
```

然后在浏览器中打开 http://127.0.0.1:8080

### 构建生产版本

```bash
cd web-app
trunk build --release --public-url ./
```



### docker 部署

```bash
# Docker nginx 部署
# 访问 http://localhost:8000
docker-compose up -d
```

构建产物将在 `dist/` 目录中，可以直接部署到静态服务器。

## 📁 项目结构

```
web-app/
├── src/
│   ├── main.rs              # 应用入口点
│   ├── app.rs               # 应用根组件和页脚
│   ├── router.rs            # 路由定义和配置
│   ├── components/          # 可复用组件
│   │   ├── mod.rs
│   │   └── nav.rs           # 导航栏组件
│   └── pages/               # 页面组件
│       ├── mod.rs
│       ├── home.rs          # 首页
│       └── about.rs         # 关于页面
├── styles/                  # Sass 样式文件
│   ├── main.scss            # 主样式入口
│   ├── _variables.scss      # 变量定义
│   ├── _mixins.scss         # 混合宏
│   ├── _base.scss           # 基础样式
│   ├── _layout.scss         # 布局样式
│   ├── _navbar.scss         # 导航栏样式
│   ├── _footer.scss         # 页脚样式
│   ├── _components.scss     # 组件样式
│   ├── _home.scss           # 首页样式
│   └── _about.scss          # 关于页面样式
├── index.html               # HTML 模板
└── Cargo.toml               # 依赖配置
```

## 🛠️ 技术栈

- **Yew 0.22** - Rust 前端框架
- **Yew Router 0.19** - 客户端路由
- **WebAssembly** - 高性能 Web 运行时
- **Sass** - CSS 预处理器
- **Trunk** - 构建工具和开发服务器

## 📄 页面说明

### 首页 (/ 或 /home)
- Hero 区域展示应用标题
- 特性卡片展示核心功能
- 统计数据展示
- 交互式计数器演示
- 响应式卡片布局

### 关于页面 (/about)
- 项目详细介绍
- 技术栈说明
- 核心特性列表
- 开发历程时间线
- 团队成员展示
- 联系方式

## 🎨 样式系统

项目使用 Sass 构建模块化的样式系统：

- **变量系统** - 统一的颜色、间距、字体大小定义
- **混合宏** - 可复用的样式模式（响应式、Flexbox、卡片等）
- **模块化** - 按功能拆分样式文件
- **响应式设计** - 移动端、平板、桌面端适配
- **动画效果** - 平滑的过渡和悬停效果

## 🔧 开发指南

### 添加新页面

1. 在 `src/pages/` 创建新的页面组件
2. 在 `src/pages/mod.rs` 中导出
3. 在 `src/main.rs` 的 `Route` 枚举中添加路由
4. 在 `switch` 函数中处理路由
5. 在 `styles/` 中创建对应的样式文件

### 自定义样式

修改 `styles/_variables.scss` 中的变量来自定义：
- 颜色主题
- 间距大小
- 字体大小
- 圆角半径
- 阴影效果
- 过渡动画

## 📦 部署

构建生产版本后，将 `dist/` 目录部署到任何静态服务器：

- Netlify
- Vercel
- GitHub Pages
- AWS S3
- Nginx

## 📝 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
